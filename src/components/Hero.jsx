import React from 'react';
import './Hero.css';

const Hero = () => {
    return (
        <section className="hero" id="home">
            <div className="hero-content">
                <h1 className="hero-title">Reveal Your Inner Beauty</h1>
                <p className="hero-subtitle">Professional Makeup Artist & Hairstylist</p>
                <button className="hero-cta">Book Appointment</button>
            </div>
        </section>
    );
};

export default Hero;
